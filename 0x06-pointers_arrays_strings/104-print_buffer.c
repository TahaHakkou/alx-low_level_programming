#include "main.h"
#include <stdio.h>
/**
 * print_buffer - prints a buffer
 * @b: a pointer to the buffer
 * @size: size of content that will be printed (bytes)
 */
void print_buffer(char *b, int size)
{
	while ()
	{
		while ()
		{
			putchar(' ');
		}
		putchar('\n');
	}
}
